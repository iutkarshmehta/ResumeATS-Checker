name: Lint

on:
  pull_request:
    branches:
      - main

jobs:
  swiftlint:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Run SwiftLint
        run: swiftlint lint --strict
